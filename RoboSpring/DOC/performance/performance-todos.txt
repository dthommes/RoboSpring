- Remove Schema and DTD Validaton logic and classes
	-> Less classes to load
	-> less Logging to init
	-> PluggableSchemaResolver
	-> BeansDtdResolver
	
- Create XML-Reader for res/xml folder
	-> About 40% faster
	-> ClassLoader.getResourceAsStream obviously checks the signature each time
	-> At the moment 11.8% of the overall process
	-> Evtl. Bufferedinputstream verwenden?
	
- ExtendedBeanInfo
	-> Method.equals ist langsam
	-> Wurde von mir aufgrund des fehlgeschlagenen Tests ja so gemacht... mmh
	
- Klassen nicht gefunden beim Laden
	-> Es gibt diverse Warnings, dass Klassen nicht gefunden werden.